import React from 'react';

export const Settings: React.FC = () => {
    return (
        <div style={{ padding: '2rem' }}>
            <h1>Settings</h1>
            <p>Application settings.</p>
        </div>
    );
};
